CONFIG_HEAP_MEM_POOL_SIZE=2048
CONFIG_IDLE_STACK_SIZE=512
CONFIG_ISR_STACK_SIZE=2048
CONFIG_MAIN_STACK_SIZE=4096
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=2048

CONFIG_REBOOT=y
CONFIG_I2C=y
CONFIG_SENSOR=y
CONFIG_PM=y
CONFIG_PM_DEVICE=y
CONFIG_PM_DEVICE_RUNTIME=y

CONFIG_SPI=y

CONFIG_NVS=y
CONFIG_FLASH=y
CONFIG_FLASH_MAP=y
CONFIG_SETTINGS=y

# Image size budget — must equal the history_partition offset (sticker.dts):
# with CONFIG_APP_HISTORY_FLASH=y (release default) the history NVS rewrites
# that area at runtime, and NVS settings + LoRaWAN NVM always rewrite the
# storage_partition. An image leaking into either region gets corrupted in
# flash → hard fault on next boot, recoverable only by mass erase. With this
# set, the linker fails the build instead ("region ROM overflowed").
# debug.conf (history kept in RAM) relaxes this to the storage offset 0x3C000.
CONFIG_FLASH_LOAD_SIZE=0x28000

CONFIG_LORA=y
CONFIG_LORAMAC_REGION_AU915=y
CONFIG_LORAMAC_REGION_EU868=y
CONFIG_LORAMAC_REGION_US915=y
CONFIG_LORAWAN_NVM_SETTINGS=y
CONFIG_LORAWAN=y
CONFIG_LORAWAN_SYSTEM_MAX_RX_ERROR=100

# Wall-clock: STM32 RTC (LSE-backed) set from LoRaWAN DeviceTimeReq. See app_clock.c.
CONFIG_RTC=y
CONFIG_RTC_STM32=y

CONFIG_LIS2DH_ACCEL_RANGE_4G=y
CONFIG_LIS2DH_BLOCK_DATA_UPDATE=y
CONFIG_LIS2DH_ODR_5=y
CONFIG_LIS2DH_OPER_MODE_LOW_POWER=y
CONFIG_LIS2DH_TRIGGER_GLOBAL_THREAD=y

CONFIG_ENTROPY_GENERATOR=y

CONFIG_WATCHDOG=y
CONFIG_WDT_DISABLE_AT_BOOT=y

CONFIG_ADC=y

CONFIG_LIS2DH=y
CONFIG_MPL3115A2=y
CONFIG_OPT3001=y
CONFIG_SHT4X=y
CONFIG_W1=y

# Flood probe (ADS122C04 resistive flood sensor behind a DS28E17 1-Wire bridge).
# Shipped in the main image like the other sensors; flip to n to reclaim flash if
# the release image (CONFIG_FLASH_LOAD_SIZE budget) ever overflows.
CONFIG_APP_FLOOD_PROBE=y

CONFIG_NET_BUF=y
CONFIG_NANOPB=y

CONFIG_MBEDTLS=y
CONFIG_MBEDTLS_PSA_CRYPTO_C=y
CONFIG_PSA_WANT_KEY_TYPE_AES=y
CONFIG_PSA_WANT_ALG_CCM=y
